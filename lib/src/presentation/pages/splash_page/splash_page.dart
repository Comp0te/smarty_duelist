import 'package:flutter/material.dart';

import '../../shared_widgets/shared_widgets.dart' show NativeScaffold;

class SplashPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return NativeScaffold(
      title: const Text(''),
      body: Container(),
    );
  }
}
